import * as React from 'react';
import { Flex, Box } from '@chakra-ui/core';

const SidePanel: React.FC = () => (
  <Flex bg="tomato" flexDirection="column">
    <Box>Things about me</Box>
  </Flex>
);

export default SidePanel;
